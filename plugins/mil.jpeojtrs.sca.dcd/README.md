# Plugin mil.jpeojtrs.sca.dcd

Provides an EMF model for the Redhawk `deviceconfiguration` descriptor (DCD) schema.

## Extension points

* Defines an Eclipse content type for XML files containing DCD content
* Adds the DTD schema to the XML editor's catalog